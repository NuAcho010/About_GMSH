# pages/interactive_geometry.py
# Страница: Интерактивные возможности создания области

import streamlit as st

def show():
    st.title("🖱️ Интерактивные возможности создания области")
    
    st.markdown("""
    ## Три способа работы с геометрией
    
    Gmsh предоставляет несколько уровней взаимодействия, которые отлично дополняют друг друга.
    """)
    
    col1, col2, col3 = st.columns(3)
    
    with col1:
        st.markdown("""
        ### 🖱️ 1. GUI
        **Графический интерфейс**
        
        - Визуальное создание
        - Мгновенная обратная связь
        - Удобно для изучения
        """)
    
    with col2:
        st.markdown("""
        ### 📝 2. Скрипты
        **`.geo` файлы**
        
        - Параметризация
        - Воспроизводимость
        - Автоматизация
        """)
    
    with col3:
        st.markdown("""
        ### 🔧 3. API
        **Программный интерфейс**
        
        - C++, C, Python, Julia
        - Максимальная гибкость
        - Интеграция в приложения
        """)
    
    st.markdown("---")
    
    st.markdown("""
    ## 🎮 Работа в GUI
    
    ### Меню Geometry
    
    При открытии Gmsh выберите `Modules -> Geometry` для доступа к инструментам:
    """)
    
    gui_steps = [
        "**Add** — создание новых элементов:",
        "   - Point — точка (клик или ввод координат)",
        "   - Straight line — прямая линия",
        "   - Spline — сплайн",
        "   - Circle/Arc — окружность/дуга",
        "   - Plane Surface — плоская поверхность",
        "   - Surface Loop — петля поверхностей",
        "   - Volume — объём",
        "",
        "**Transform** — трансформации:",
        "   - Translate — перенос",
        "   - Rotate — вращение",
        "   - Scale — масштабирование",
        "",
        "**Boolean** — булевы операции:",
        "   - Union — объединение",
        "   - Intersection — пересечение",
        "   - Difference — разность"
    ]
    
    for step in gui_steps:
        st.markdown(step)
    
    st.markdown("---")
    
    st.markdown("""
    ## 🔗 Связь GUI и скриптов
    
    **Самая мощная функция Gmsh** — автоматическая генерация кода!
    
    Каждое действие в GUI:
    1. Немедленно отображается на экране
    2. **Автоматически добавляет соответствующую команду** в открытый `.geo` файл
    3. Можно сохранить и использовать как скрипт
    """)
    
    st.success("""
    ### Пример workflow:
    
    1. Открыть Gmsh
    2. Создать новый `.geo` файл
    3. Включить запись действий (по умолчанию включена)
    4. Нарисовать геометрию мышкой
    5. Сохранить файл — получить готовый скрипт!
    6. Изучить код и добавить параметры
    """)
    
    st.markdown("---")
    
    st.markdown("""
    ## ⌨️ Горячие клавиши для геометрии
    
    | Клавиша | Действие |
    |---------|----------|
    | **e** | Завершить создание текущего элемента |
    | **q** | Отменить создание |
    | **x, y, z** | Заморозить координату при создании |
    | **Shift+x/y/z** | Двигаться только по оси |
    | **0** | Перезагрузить геометрию из скрипта |
    """)
    
    st.markdown("---")
    
    st.markdown("""
    ## 💡 Интерактивное создание области — пошагово
    
    ### Способ 1: Ручное построение
    1. `Add -> Point` — создать 8 точек
    2. `Add -> Straight line` — соединить их 12 линиями
    3. `Add -> Curve Loop` — создать 6 петель (выбрать линии в правильном порядке)
    4. `Add -> Plane Surface` — создать 6 поверхностей
    5. `Add -> Surface Loop` — выбрать все 6 поверхностей
    6. `Add -> Volume` — создать объём
    
    ### Способ 2: Экструзия (гораздо проще!)
    1. Создать квадратную поверхность (4 точки + 4 линии + петля + поверхность)
    2. `Transform -> Extrude` — выбрать поверхность, задать вектор выдавливания
    3. Объём создаётся автоматически!
    """)
    
    code_extrude = """
    // Код, который сгенерирует GUI при экструзии:
    Extrude {0,0,1} { Surface{1}; Layers{10}; Recombine; }
    """
    st.code(code_extrude, language="python")

# Для автономного тестирования
if __name__ == "__main__":
    show()
